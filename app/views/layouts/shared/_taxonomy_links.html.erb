<% @taxonomies.each_with_index do |taxonomy, tab_index| %>
  <% cache [I18n.locale, taxonomy, max_level, @taxon] do %>
    <li class="mv-nav-item" role="menuitem" data-taxonomy-id="<%= taxonomy.id %>">
      <%= link_to Spree.t(:shop_by_taxonomy, taxonomy: taxonomy.name), anchor: taxonomy.name, tab_index: tab_index %>
      <ul class="dl-submenu">
        <%= taxons_accordion(taxonomy.root, @taxon, max_level) %>
      </ul>
    </li>
  <% end %>
<% end %>

<!-- <%# This was re-added to header on 7-3-2016 to begin styling on mobile side. %> -->
<%#= render partial: 'spree/shared/login_bar' if home_ip? %>
